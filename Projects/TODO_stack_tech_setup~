TODO: 
* all projects need to be fixed in their test and spec files so that they run via script
* note: hangmanproject + phoneproject didn't even pass the build mode, so we can't even do stack exec .exe
